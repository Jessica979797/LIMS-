1. claude code 启动前后端项目    启前端项目时 切换node 环境 导致第二天 Claude code  不能用  重新装claude  包即可

2. 学习启动mysql  redis 服务   & "D:\mysql-8.4.9-winx64\bin\mysqld.exe" --defaults-file=D:\mysql-8.4.9-winx64\my.ini --console       & "D:\redis\redis-server.exe" --port 6379

3.  使用DBeaver  操作数据库     导入lims库  输入库名以及地址端口号 密码，  f3   写sql  select  * from lime limit 10   ctrl  + enter  双击修改库 点击保存即可

4. 前端怎末进行技术选型  后端怎末进行技术选型   

5. node 学习    buffer  / stream      

6. 性能优化  // 使用闭包缓存结果 function memoize(fn) {   const cache = new Map();   return (...args) => {     const key = JSON.stringify(args);     if (cache.has(key)) return cache.get(key);     const result = fn(...args);     cache.set(key, result);     return result;   }; }    

7. ![1785648045092](C:\Users\wrr\AppData\Local\Temp\1785648045092.png)

    Thunder Client   测接口  类似的 postman   xapi

8. 怎末学习后端开发  从nodejs 开始   启nodejs 服务   express 服务    data.json   mysql   prisma  jwt 

9. :Node 基础 -> Express -> CRUD -> MySQL -> 持久化 -> JWT -> Prisma -> 部署。后端开发者的核心技能树

https://ecs.console.aliyun.com/server/i-uf6brac4ty4oqgr5pl4q/detail?regionId=cn-shanghai
https://github.com/Jessica979797/LIMS-/blob/main/web/src/app.tsx
10.  阿里云部署   ls -la /root
 日常运维(在 /root/LIMS- 目录):
  docker compose logs -f server     # 看后端日志
  docker compose restart server     # 重启后端
  docker compose up -d --build      # 改了代码后重新构建启动
  docker compose down               # 停全部(数据卷保留)

  docker compose up -d
  docker compose ps
  docker compose logs server

  cd /root/LIMS-
  以后更新代码的标准流程(不用重启服务器)

  cd /root/LIMS-
  git pull                              # 1. 先拉新代码
  docker compose up -d --build          # 2. 再构建启动
  然后浏览器 Ctrl+F5。

  开个无痕/隐私窗口打开 http://47.102.154.156/login   无痕窗口怎么打開
  - 无痕里是新版(有语言切换)= 普通窗口缓存问题,原窗口 Ctrl+F5 强刷就行
  - 无痕里还是旧版 = build 没生效,在服务器上跑无缓存重建:
  docker compose build --no-cache web
  docker compose up -d
  (无缓存重建 web,几分钟)再 Ctrl+F5 刷浏览器

   部署完成清单

  ┌───────────────────────┬──────────────────────────────────┐
  │          项           │               状态               │
  ├───────────────────────┼──────────────────────────────────┤
  │ 前端 + nginx          │ ✅ 运行中                        │
  ├───────────────────────┼──────────────────────────────────┤
  │ NestJS 后端           │ ✅ 运行中(:3000)                 │
  ├───────────────────────┼──────────────────────────────────┤
  │ MySQL + Redis + MinIO │ ✅ 运行中                        │
  ├───────────────────────┼──────────────────────────────────┤
  │ 访问                  │ ✅ http://47.102.154.156/ 能登录 │
  ├───────────────────────┼──────────────────────────────────┤
  │ 数据库迁移            │ ✅ 表已建                        │
  ├───────────────────────┼──────────────────────────────────┤
  │ 自动备份              │ ✅ 每天 3 点,留 7 天,/root/bk/   │
  ├───────────────────────┼──────────────────────────────────┤
  │ 部署方案 + 踩坑       │ ✅ 已记入记忆                    │
  └───────────────────────┴──────────────────────────────────┘

    MySQL 数据在 mysql-data 卷里。定期备份:
  docker compose exec mysql mysqldump -uroot -pLimsMysql2026ab lims > backup.sql

  
  最可能的原因:你第一次 docker compose up -d --build 是在 git pull 之前跑的--构建用的是服务器上的旧代码,所以
  build 出来还是旧版。后来代码拉下来了,但容器没重建(或浏览器缓存),一直看到旧的。

  重启服务器为什么管用:重启后所有容器干净重启,加上之前 no-cache 重建的新镜像,web
  容器用上了新代码,所以好了。重启算是"一锤定音"把状态全刷新了。

  关键:看 up -d 输出里 web 是不是显示 Recreating lims-web:
  - 显示 Recreating ... done = 用上新镜像了 ✅
  - 只显示 Running 没重建 = 镜像没变,新代码没进去;这时跑 docker compose build --no-cache web && docker
  compose up -d 强制重建